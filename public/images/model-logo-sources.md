Model logo card paths are adapted from `@lobehub/icons` 5.8.0 (MIT), copied into local SVG cards so the game does not depend on remote image loading.

Source: https://www.npmjs.com/package/@lobehub/icons

The Doubao card uses the public App Store app icon image and wraps it in the same local card frame.
